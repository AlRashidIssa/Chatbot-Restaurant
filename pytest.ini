[pytest]
# Ignore deprecation warnings globally
filterwarnings =
    ignore::DeprecationWarning

# Specify a pattern for test files (optional, pytest already defaults to this behavior)
python_files = test_*.py
